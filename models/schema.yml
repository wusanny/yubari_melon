version: 2

models:
  - name: dim_customers
    config:
      materialized: table
    columns:
      - name: customer_id
        description: This is the primary key
        data_type: int
      - name: country_name
        description: Where this customer lives
        data_type: varchar
